@model IEnumerable<LogisticsProject.Models.Przewoznicy>

@{
    ViewBag.Title = "Przewoźnicy";
}

<h2><br />Przewoźnicy</h2>

<p>
    @Html.ActionLink("Utwórz", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayName("Adres Kontaktowy")
        </th>
        <th>
            @Html.DisplayName("Właściciel")
        </th>
        <th>
            @Html.DisplayName("Nr Komorkowy")
        </th>
        <th>
            @Html.DisplayName("Nr Konta")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdresKontaktowy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Wlasciciel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NrKomorkowy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NrKonta)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Szczegóły", "Details", new { id = item.ID }) |
            @Html.ActionLink("Usuń", "Delete", new { id = item.ID })
        </td>
    </tr>
}

</table>
